import 'package:flutter/material.dart';

/// الدرجات والتقارير — تبويب بالشريط السفلي.
/// 🧹 تم تفريغ محتوى هالتبويب بالكامل بناءً على الطلب (تحضيراً لمحتوى
/// جديد لاحقاً). الشاشة بقيت موجودة فاضية حتى ما ينكسر أي مكان بيستدعيها
/// (home_shell.dart وhome_screen.dart).
class GradesReportsScreen extends StatelessWidget {
  const GradesReportsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
